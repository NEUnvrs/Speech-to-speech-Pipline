from os import name
from struct import pack
from setuptools import setup, find_packages

setup(name='speech-to-speech',
    version='0.1',
    packages=find_packages(),
    description='Speech-to-Speech conversational AI program',
    author='NEU',)